# 统一换行符处理，避免不同平台克隆时产生无意义的整文件改动
#
# 背景：本项目在 Windows 上开发（源码为 LF），若不规范，
#       Git 会在每次操作时输出大量 "LF will be replaced by CRLF" 警告，
#       且其他平台克隆后可能整文件 diff。

# 默认：交由 Git 自动判断，仓库内统一存为 LF
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则 cmd.exe 解析异常
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不做换行符转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.whl  binary
*.pkl  binary
*.exe  binary

# 锁文件不参与 diff 统计（体量大且无需逐行审查）
package-lock.json -diff linguist-generated
