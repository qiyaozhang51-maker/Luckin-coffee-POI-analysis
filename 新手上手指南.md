# 🍵 瑞幸咖啡门店选址与POI时空演变分析平台 — 新手上手指南

> **适合人群**：第一次接触本项目的开发者、GIS初学者、面试展示前的快速了解者。
> **预计阅读时间**：30 分钟（通读） / 2 小时（边读边跑）

---

## 目录

1. [这个项目是做什么的？](#1-这个项目是做什么的)
2. [系统是怎么跑起来的？](#2-系统是怎么跑起来的)
3. [代码在哪里？— 项目文件地图](#3-项目文件地图)
4. [数据库里有什么？](#4-数据库里有什么)
5. [后端提供了哪些接口？](#5-后端提供了哪些接口)
6. [前端每个页面是干什么的？](#6-前端每个页面是干什么的)
7. [数据分析是怎么做的？](#7-数据分析是怎么做的)
8. [选址预测模型是怎么工作的？](#8-选址预测模型是怎么工作的)
9. [怎么在本地跑起来？](#9-怎么在本地跑起来)
10. [常见问题排查](#10-常见问题排查)
11. [如果想二次开发？](#11-如果想二次开发)

---

## 1. 这个项目是做什么的？

### 一句话概括

用 **地图 + 数据 + AI** 分析瑞幸咖啡 2 万多家门店的选址规律，并训练一个模型来预测哪里适合开新店。

### 核心功能（4 个模块）

```
┌────────────────────────────────────────────────────────┐
│                                                        │
│   🗺️ 时空演变           🎯 POI 关联分析               │
│   门店从2017到2026年     写字楼、地铁站、商场...        │
│   怎么在全国扩张的？      对选址的影响有多大？           │
│                                                        │
│   ⚔️ 竞品对比           🤖 选址预测                   │
│   瑞幸 vs 星巴克         在地图上点一个位置              │
│   选址策略有什么不同？     AI 告诉你适不适合开店          │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 技术价值

| 维度 | 说明 |
|------|------|
| **学术** | 商业地理学 + 空间数据科学交叉研究，可发表 GIS/城市规划方向论文 |
| **商业** | 选址策略量化分析，对新零售企业有参考价值 |
| **求职** | 全栈 GIS 项目：数据采集 → 空间分析 → ML 建模 → Web 可视化 |

---

## 2. 系统是怎么跑起来的？

### 从用户点击到数据显示，完整链路：

```
你在浏览器打开 http://localhost:3000
         │
         ▼
┌──────────────────────────────────┐
│  React 前端 (Vite 开发服务器)     │
│  端口 3000                        │
│                                   │
│  你看到的：                        │
│  - 地图（deck.gl 渲染2万+点位）    │
│  - 图表（ECharts）                │
│  - 各种筛选器和交互控件            │
└──────────┬───────────────────────┘
           │ 前端发出请求: GET /api/stores?brand=luckin&city=北京
           │
           ▼  Vite Proxy 把 /api/* 请求转发给后端
┌──────────────────────────────────┐
│  FastAPI 后端 (Python + uvicorn)  │
│  端口 8000                        │
│                                   │
│  后端做的事：                      │
│  - 接收请求，解析参数              │
│  - 拼 SQL 语句                    │
│  - 通过 asyncpg 发给数据库         │
│  - 把数据库返回的原始数据           │
│    整理成 JSON 格式返回给前端       │
└──────────┬───────────────────────┘
           │ SELECT * FROM stores WHERE ...
           │
           ▼
┌──────────────────────────────────┐
│  PostgreSQL + PostGIS 空间数据库  │
│  端口 5432                        │
│                                   │
│  存着所有数据：                    │
│  - stores: ~13,000 条门店         │
│  - pois: ~32,468 条 POI           │
│  - store_poi_stats: 预计算的关联   │
│  - 各种缓存表和物化视图            │
└──────────────────────────────────┘
```

### 关键问题解答

**Q: 前端怎么找到后端的？**
在开发环境中，Vite 配置了代理（`vite.config.ts` 第 10-14 行）。前端请求 `/api/*` 路径时，Vite 自动把请求转给 `http://localhost:8000`。这样前端开发时不需要跨域配置。

**Q: 后端怎么连数据库的？**
通过 `asyncpg`（Python 异步 PostgreSQL 驱动）。连接信息存在 `.env` 文件里，`backend/database.py` 读取并建立连接。

**Q: 为什么不用普通数据库而要用 PostGIS？**
因为我们需要计算"这家门店 500 米范围内有多少写字楼"这种空间查询。普通数据库做不到，PostGIS 的 `ST_DWithin` 函数可以做，而且 GiST 空间索引让它很快。

---

## 3. 项目文件地图

当你第一次克隆项目，看到这个目录结构。下面标注了每个重要文件的职责：

```
luckin-spatial-analysis/
│
├── 📄 README.md                    ← 项目简介（英文版）
├── 📄 新手上手指南.md               ← 你正在读的这个文件 ⭐
├── 📄 项目方案文档.md               ← 完整技术方案（长篇）
│
├── 🐳 docker-compose.yml           ← Docker 一键部署配置
├── 🐳 Dockerfile (backend/frontend)
│
├── 📄 .env.example                 ← 环境变量模板
├── 📄 requirements.txt             ← Python 依赖列表（含数据分析）
├── 📄 requirements-api.txt         ← Python 依赖（仅后端API，精简版）
│
├── 📁 data/                        ← 存放原始数据、模型文件
│   ├── raw/                          高德API采集的原始JSON
│   ├── processed/                    处理后的中间数据
│   └── models/                       训练好的 XGBoost 模型 (.pkl)
│
├── 📁 sql/                         ← 数据库建表和分析SQL
│   ├── schema.sql                    建表语句（7张表 + 索引）
│   ├── spatial_join.sql              空间查询函数（门店-POI关联）
│   └── analytics.sql                 分析查询（时间序列、聚类、排名）
│
├── 📁 scripts/                     ← 数据采集与特征计算脚本
│   ├── config.py                    全局配置（API Key、城市列表、POI映射）
│   ├── fetch_stores.py              从高德API采集门店数据
│   ├── fetch_pois.py                从高德API + OSM 采集POI数据
│   ├── import_osm.py                从OpenStreetMap导入道路/建筑/地铁
│   ├── compute_features.py          计算门店-POI空间关联特征
│   └── train_model.py               训练XGBoost选址预测模型
│
├── 📁 backend/                     ← FastAPI 后端服务
│   ├── main.py                      FastAPI 入口，挂载路由
│   ├── database.py                  数据库连接配置
│   ├── routers/
│   │   ├── stores.py                门店相关 API（列表、热力图、时间轴）
│   │   ├── analysis.py              POI 分析 API（关联、Lift值、品牌对比）
│   │   ├── prediction.py            选址预测 API（评分、热力图）
│   │   └── geo.py                   逆地理编码 API（经纬度→城市名）
│   └── models/
│       └── schemas.py               Pydantic 数据模型定义
│
├── 📁 frontend/                    ← React 前端应用
│   ├── index.html                   Vite 入口 HTML
│   ├── vite.config.ts               Vite 配置（含API代理）
│   ├── package.json                 前端依赖列表
│   ├── src/
│   │   ├── main.tsx                  React 入口（挂载 App）
│   │   ├── App.tsx                   路由和页面布局
│   │   ├── pages/
│   │   │   ├── PoiAnalysisPage.tsx   POI 关联分析页面
│   │   │   └── ComparisonPage.tsx    瑞幸 vs 星巴克对比页面
│   │   ├── components/
│   │   │   ├── TimelineSlider.tsx    时间轴滑块组件
│   │   │   └── StorePopup.tsx        门店弹窗组件
│   │   ├── hooks/                    自定义 React Hooks
│   │   │   ├── useApi.ts            通用 HTTP 请求 Hook
│   │   │   ├── useCities.ts          城市列表
│   │   │   ├── useStoreData.ts       门店数据（时间轴、热力图）
│   │   │   ├── usePoiAnalysis.ts     POI 分析数据
│   │   │   ├── useComparison.ts      品牌对比数据
│   │   │   └── usePrediction.ts      选址预测
│   │   ├── styles/global.css         全局样式
│   │   └── vite-env.d.ts            TypeScript 类型声明
│   └── tsconfig.json
│
└── 📁 notebooks/                   ← Jupyter 分析笔记本
    ├── 01_data_exploration.ipynb    数据探索
    ├── 02_spatial_analysis.ipynb    空间分析
    ├── 03_poi_correlation.ipynb     POI 关联分析
    └── 04_prediction_model.ipynb   选址预测
```

---

## 4. 数据库里有什么？

### 7 张核心表 + 3 张辅助表/视图

**数据库备份文件**：`data/luckin_spatial_backup.sql`（34MB）包含全部门店、POI 和预计算数据，可通过以下命令恢复：

```bash
psql -U luckin -h localhost -d luckin_spatial -f data/luckin_spatial_backup.sql
```

#### 表关系图

```
┌────────────┐     ┌───────────────┐     ┌────────────┐
│   stores   │────→│store_poi_stats│←────│    pois    │
│   门店表    │     │  门店-POI关联  │     │   POI表    │
│  ~13,000条 │     │  预计算缓存    │     │   百万级    │
└─────┬──────┘     └───────────────┘     └─────┬──────┘
      │                                       │
      │  city 字段关联                         │  city 字段关联
      ▼                                       ▼
┌────────────┐                        ┌───────────────┐
│ city_tiers │                        │admin_boundaries│
│  城市层级   │                        │   行政边界     │
│   20条     │                        │  ~3,000条     │
└────────────┘                        └───────────────┘
```

#### 表详解

**① stores — 门店表（最重要）**

```sql
stores (
    id, name,          -- 编号、店名
    brand,             -- 'luckin' 或 'starbucks'
    lng, lat,          -- 经纬度坐标
    geom,              -- PostGIS 空间点（由 lng/lat 自动生成）
    city, district,    -- 城市、区
    address,           -- 详细地址
    open_date,         -- 开业日期
    store_type,        -- 快取店/悠享店/外卖厨房/旗舰店
    status,            -- 营业中/已关闭
    confidence,        -- 开业日期可信度 (0-1)
    data_source        -- 数据来源
)
```

关键索引：**GiST 空间索引**（`idx_stores_geom`）是性能核心，让"查找某点附近500m内所有门店"从秒级降到毫秒级。

**② pois — POI 表**

```sql
pois (
    id, name, category,   -- 编号、名称、分类（office/mall/metro/...）
    lng, lat, geom,       -- 坐标
    city, district,       -- 城市
    data_source,          -- 'amap' 或 'osm'
    amap_typecode         -- 高德原始分类编码
)
```

POI 分为 7 大类：`office`（办公）、`mall`（商业）、`metro`/`bus`（交通）、`university`/`school`（教育）、`residential`（居住）、`restaurant`/`cafe`（餐饮）、`convenience`/`bank`/`hospital`（生活）。

**③ store_poi_stats — 门店-POI 关联缓存表（性能关键）**

```sql
store_poi_stats (
    store_id,       -- 门店ID
    radius,         -- 半径：200 / 500 / 1000 米
    poi_category,   -- POI分类
    poi_count       -- 该半径内该类POI的数量
)
```

**为什么需要这张表？** 如果不缓存，每次 API 请求都要实时跑 `ST_DWithin` 空间关联查询（百万 POI × 万级门店 ≈ 非常慢）。预计算后存到这张表，API 直接读结果，毫秒级响应。

**④ city_tiers — 城市层级表**

| 城市 | 层级 | 标签 | 人口 | GDP |
|------|------|------|------|-----|
| 北京 | 1 | 一线 | 2189万 | 4376亿 |
| 成都 | 2 | 新一线 | 2094万 | 2202亿 |
| ... | ... | ... | ... | ... |

20 个重点分析城市，按一线/新一线/二线分级，附带人口和 GDP 数据，用于分析城市层级对选址的影响。

**⑤⑥⑦ 辅助表和视图**

| 表/视图 | 用途 |
|---------|------|
| `admin_boundaries` | 全国省市县行政边界（GeoJSON 多边形），用于地图绘制和范围限定 |
| `kde_grid` | 核密度分析网格缓存，预计算门店空间密度 |
| `prediction_grid` | 选址预测网格缓存，预计算城市网格评分 |
| `random_points` | LIFT 值计算的对照组：在城市范围内随机生成的点 |
| `mv_city_stats` | **物化视图**：每个城市的门店汇总，定时刷新（不用每次查询都 GROUP BY） |

---

## 5. 后端提供了哪些接口？

### API 地图

所有接口都在 `http://localhost:8000` 下。启动后端后，打开 `http://localhost:8000/docs` 可以看到 Swagger 交互式文档。

### 门店接口 (`/api/stores`)

| 方法 | 路径 | 干什么的 | 参数示例 |
|------|------|---------|---------|
| `GET` | `/api/stores` | 门店列表（分页） | `?brand=luckin&city=北京&limit=50&offset=0` |
| `GET` | `/api/stores/{id}` | 某家门店的详情 + 周边POI | `123` |
| `GET` | `/api/stores/nearest` | 根据坐标查找最近门店（KNN） | `?lng=121.47&lat=31.23&brand=luckin` |
| `GET` | `/api/stores/heatmap/data` | 热力图 GeoJSON | `?brand=luckin&city=上海&year=2024` |
| `GET` | `/api/stores/timeline/data` | 每月新增门店数 | `?brand=luckin&city=成都` |
| `GET` | `/api/stores/cities/list` | 各城市门店数量 | 无参数 |

### 分析接口 (`/api/analysis`)

| 方法 | 路径 | 干什么的 | 参数示例 |
|------|------|---------|---------|
| `GET` | `/api/analysis/store-poi-correlation` | 门店周边各类POI平均数量 | `?brand=luckin&radius=500&city=上海` |
| `GET` | `/api/analysis/lift-scores` | 各类POI的Lift值 | `?brand=luckin&radius=500` |
| `GET` | `/api/analysis/brand-comparison` | 瑞幸vs星巴克对比 | `?city=北京` |
| `GET` | `/api/analysis/city-tiers-stats` | 按城市层级统计 | 无参数 |

### 预测接口 (`/api/prediction`)

| 方法 | 路径 | 干什么的 | 参数 |
|------|------|---------|------|
| `POST` | `/api/prediction/score` | 给一个位置打分 | `{"lng": 121.47, "lat": 31.23, "city": "上海"}` |
| `GET` | `/api/prediction/heatmap` | 整个城市的预测热力图 | `?city=北京&grid_size_m=500` |

### 地理编码接口 (`/api/geo`)

| 方法 | 路径 | 干什么的 | 参数 |
|------|------|---------|------|
| `GET` | `/api/geo/reverse` | 经纬度反查城市名 | `?lng=121.47&lat=31.23` |

### 请求/响应示例

**请求：给上海市中心一个位置打分**
```bash
curl -X POST http://localhost:8000/api/prediction/score \
  -H "Content-Type: application/json" \
  -d '{"lng": 121.4737, "lat": 31.2304, "city": "上海"}'
```

**响应：**
```json
{
  "score": 0.82,
  "lng": 121.4737,
  "lat": 31.2304,
  "city": "上海",
  "features": {
    "metro_500": 3,
    "office_500": 12,
    "mall_500": 2,
    "restaurant_500": 45
  },
  "top_positive": [
    {"feature": "office_500", "value": 12, "importance": 0.28},
    {"feature": "metro_500", "value": 3, "importance": 0.18},
    {"feature": "restaurant_500", "value": 45, "importance": 0.12}
  ],
  "top_negative": [],
  "model_available": true
}
```

**解读**：这个位置得分 0.82（满分 1.0），非常适合开瑞幸。主要加分项是周边 500m 内有 12 栋写字楼和 3 个地铁站。

---

## 6. 前端每个页面是干什么的？

### 页面导航

前端是一个 React 单页应用（SPA），使用 Ant Design 组件库，有以下几个主要页面：

### 首页 — 总览仪表盘

```
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 瑞幸门店  │ │ 星巴克    │ │ 覆盖城市  │ │ 年均增长  │
│ 21,847   │ │  6,892   │ │   328    │ │   42%    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘

┌─────────────────────────────────────────────┐
│          全国门店分布总览地图                  │
│  蓝色散点 = 瑞幸  绿色散点 = 星巴克           │
│  可以缩放、拖拽、点击看详情                    │
└─────────────────────────────────────────────┘
```

**技术实现**：deck.gl 的 `ScatterplotLayer` 用 GPU 渲染 2 万+ 点位，普通浏览器也能跑 30fps。

### POI 分析页

```
┌──────────────────────┐  ┌──────────────────────┐
│   POI 雷达图          │  │   POI Lift 值         │
│   展示各类POI在门店     │  │   柱状图展示哪些POI    │
│   周边的平均密度        │  │   类型在门店附近富集    │
│                      │  │                      │
│       metro          │  │  office  ████████ 2.3│
│      /     \         │  │  metro   ███████  1.8│
│   bus       office   │  │  mall    █████    1.5│
│      \     /         │  │  cafe    ████     1.2│
│     residential      │  │  ------ 基准线 ------  │
└──────────────────────┘  └──────────────────────┘

┌──────────────────────────────────────────────┐
│  统计详情表格：每个POI类别的均值、标准差、最大值  │
└──────────────────────────────────────────────┘
```

**Lift 值是什么意思？**

> Lift = 门店周边某类POI的密度 ÷ 随机位置同类POI的密度
>
> - Lift > 1.5 → 该类POI是门店选址的关键吸引因素
> - Lift ≈ 1.0 → 没什么特别关系
> - Lift < 0.8 → 门店倾向于避开该类POI

**关键发现**：写字楼（Lift 2.3）和地铁站（Lift 1.8）是瑞幸选址最强的两个信号。

### 竞品对比页

```
┌──────────────────────────────────────┐
│  🏙️ 选择城市：[北京 ▼]               │
├──────────────────────────────────────┤
│                                      │
│  ┌─────────┐  ┌─────────┐           │
│  │ 瑞幸     │  │ 星巴克   │           │
│  │ 1,234家 │  │  456家  │           │
│  └─────────┘  └─────────┘           │
│                                      │
│  共址率（200m内）：18.5%              │
│                                      │
│  ┌──────────────────────────────┐    │
│  │ 按城市层级的门店分布           │    │
│  │ 一线  ████████  ████         │    │
│  │ 新一线 █████████ ██████       │    │
│  │ 二线  ██████    ███          │    │
│  │      (蓝=瑞幸 绿=星巴克)       │    │
│  └──────────────────────────────┘    │
└──────────────────────────────────────┘
```

**核心发现**：瑞幸在二三线城市的门店占比（~35%）远高于星巴克（~15%），体现了瑞幸的"下沉市场"策略。

### 选址预测页

```
┌──────────────────────────────────────────────┐
│  在地图上点击一个位置，或者在搜索框输入地址      │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │           地图（可点击选点）            │    │
│  │                                      │    │
│  │              📍                      │    │
│  │                                      │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  🌟 选址评分：0.82 / 1.0         🟢 推荐     │
│                                              │
│  👍 加分项：                                  │
│  office_500 (12栋写字楼): +0.28              │
│  metro_500 (3个地铁站): +0.18               │
│  restaurant_500 (45家餐厅): +0.12            │
│                                              │
│  👎 减分项：                                  │
│  (无)                                        │
└──────────────────────────────────────────────┘
```

**技术细节**：
- 点击地图获取坐标 → 调用高德逆地理编码 API 获取城市名
- 将坐标和城市名发送到后端 `/api/prediction/score`
- 后端查询该位置周边各类 POI 数量 → 输入模型 → 返回评分和特征贡献

### 前端组件关系

```
main.tsx (React 入口)
  └── App.tsx (路由 + 导航)
        ├── 首页 (全国门店地图 + 统计卡片)
        ├── 时空演变页 (时间轴 + 热力图)
        ├── PoiAnalysisPage.tsx
        │     ├── usePoiAnalysis hook → /api/analysis/*
        │     ├── useCities hook → /api/stores/cities/list
        │     └── ECharts 雷达图 + 柱状图 + AntD 表格
        ├── ComparisonPage.tsx
        │     ├── useComparison hook → /api/analysis/brand-comparison
        │     └── ECharts 柱状图 + AntD 统计卡片
        └── 选址预测页
              └── usePrediction hook → POST /api/prediction/score
```

---

## 7. 数据分析是怎么做的？

### 7.1 数据采集流程

```
第一步：采集门店                    第二步：采集POI
┌─────────────────┐              ┌─────────────────┐
│ fetch_stores.py │              │  fetch_pois.py  │
│                 │              │                 │
│ 高德 POI API    │              │ 高德 POI API    │
│ 搜"瑞幸咖啡"    │              │ 搜写字楼、商场…  │
│ + "星巴克"      │              │ (20城精细采集)  │
│ ↓               │              │                 │
│ 去重、清洗       │              │ + import_osm.py │
│ ↓               │              │ OSM 道路/地铁    │
│ stores 表       │              │ (全国基础覆盖)  │
│ ~13,000条       │              │ ↓               │
└─────────────────┘              │ pois 表         │
                                 │ ~百万条          │
                                 └─────────────────┘
                                           │
                          第三步：空间关联计算  │
                         ┌───────────────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ compute_features.py │
              │                     │
              │ 对每个门店，计算      │
              │ 200m/500m/1000m     │
              │ 半径内的各类POI数量   │
              │ ↓                   │
              │ store_poi_stats 表   │
              │ 预计算缓存           │
              └─────────┬───────────┘
                        │
             第四步：训练模型
                        │
                        ▼
              ┌─────────────────────┐
              │  train_model.py     │
              │                     │
              │ 正样本: 门店位置     │
              │ 负样本: 随机位置     │
              │ → XGBoost 模型      │
              │ → .pkl 文件         │
              └─────────────────────┘
```

### 7.2 Lift 值是怎么算的？

这是本项目最重要的分析指标。举个例子：

1. **选对照组**：在 20 个城市的行政边界内，随机生成 20,000 个点
2. **算门店侧**：对每个瑞幸门店，数它 500m 内有几个地铁站 → 平均 2.3 个
3. **算随机侧**：对每个随机点，数它 500m 内有几个地铁站 → 平均 1.1 个
4. **Lift = 2.3 / 1.1 = 2.09**

解读：在瑞幸门店附近遇到地铁站的概率，是在城市里随便走走遇到地铁站的 2.09 倍。说明地铁站确实是瑞幸选址的重要考虑因素。

### 7.3 三个半径分别代表什么？

| 半径 | 步行时间 | 对应场景 | 分析价值 |
|------|---------|---------|---------|
| 200m | 1-2 分钟 | 出门就到 | 判断门店的"即时可达性" |
| 500m | 5-7 分钟 | 用户愿意自提的距离 | **最核心的选址分析半径** |
| 1000m | 10-15 分钟 | 外卖配送核心范围 | 判断门店的"外卖覆盖面" |

---

## 8. 选址预测模型是怎么工作的？

### 8.1 问题定义

这是一个 **二分类问题**：
- **正样本（label=1）**：现有瑞幸门店位置（~18,000 个）
- **负样本（label=0）**：同城市随机生成的位置（~18,000 个）
- **目标**：判断一个位置"像不像瑞幸会开的地方"

### 8.2 特征体系（28 个特征）

每个位置提取的特征 = **9 类 POI × 3 个半径 + 1 个城市层级**：

```
特征向量
  ├── metro_200    metro_500    metro_1000     ← 地铁站
  ├── bus_200      bus_500      bus_1000       ← 公交站
  ├── office_200   office_500   office_1000    ← 写字楼
  ├── mall_200     mall_500     mall_1000      ← 购物中心
  ├── restaurant_200 ...                       ← 餐厅
  ├── cafe_200     ...                         ← 咖啡厅
  ├── residential_200 ...                      ← 住宅
  ├── university_200 ...                       ← 大学
  ├── convenience_200 ...                      ← 便利店
  └── city_tier                                ← 城市层级(1-4)
```

### 8.3 为什么选 XGBoost？

| 算法 | 准确率 | 训练速度 | 能解释吗？ |
|------|--------|---------|----------|
| 逻辑回归 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ 很清楚 |
| 随机森林 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐ 一般 |
| **XGBoost** | **⭐⭐⭐⭐** | **⭐⭐⭐** | **✅ SHAP** |
| 神经网络 | ⭐⭐⭐⭐ | ⭐ | ❌ 黑箱 |

XGBoost 在"准确率 + 可解释性"之间平衡最好。我们用 SHAP 可以解释每个特征对评分的贡献。

### 8.4 评分流程（当你在前端点一个位置时）

```
用户点击地图 (lng=121.47, lat=31.23)
         │
         ▼
  后端接收请求
         │
         ├──→ 查询 pois 表: 这个位置 200m/500m/1000m 内各类POI数量
         │    生成 27 个 POI 特征
         │
         ├──→ 查询 city_tiers 表: 城市层级 = ?
         │    得到 city_tier 特征
         │
         ├──→ 组装成特征向量 [3, 5, 8, 12, 2, 3, ... , 1]
         │
         ├──→ 输入 XGBoost 模型 → 原始分数
         │
         ├──→ Sigmoid 归一化到 0-1
         │
         └──→ 返回 JSON: {score: 0.82, features: {...}, top_positive: [...]}
```

### 8.5 当前实现的简化版

由于 XGBoost 模型文件可能未训练/丢失，当前后端代码里有一个**基于权重的兜底方案**（`backend/routers/prediction.py` 第 79-96 行）：

```python
# 手动设置的权重（来自 Lift 分析结论）
weights = {
    'office_500': 0.25,      # 写字楼最重要
    'metro_500': 0.20,       # 地铁站其次
    'mall_500': 0.15,
    'residential_500': 0.15,
    'university_500': 0.10,
    'restaurant_500': 0.05,
    'convenience_500': 0.05,
    'cafe_500': 0.05,
}
# 加权求和 → Sigmoid 归一化
```

如果模型文件存在，后端会自动加载使用；不存在则降级为权重方案。

---

## 9. 怎么在本地跑起来？

### 方法一：Python 管理控制台（推荐）

```bash
cd luckin-spatial-analysis

# 交互式菜单（引导式操作）
python manage.py

# 或一键启动全部服务
python manage.py start

# 或双击（Windows）
启动服务.bat          # 一键启动全部服务
控制台.bat            # 打开交互式管理控制台
```

管理控制台常用命令：
- `python manage.py start` -- 启动全部服务
- `python manage.py stop` -- 停止全部服务
- `python manage.py status` -- 查看运行状态

### 方法二：手动启动（开发者常用）

#### 前置条件

- Python 3.11+ (`python --version`)
- Node.js 20+ (`node --version`)
- PostgreSQL 15 + PostGIS 3.3（本地安装或用 Docker 只跑数据库）

#### 步骤

```bash
# 1. 进入项目目录
cd luckin-spatial-analysis

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env，填入你的高德 API Key: AMAP_API_KEY=你的key

# 3. 安装 Python 依赖
pip install -r requirements-api.txt

# 4. 启动数据库（二选一）
#    A) Docker 只跑 PostGIS:
#       docker compose up -d postgis
#    B) 本地 PostgreSQL 已安装:
#       确保 pg_isready 返回正常，数据库 luckin_spatial 已创建

# 5. 初始化数据库（使用 manage.py 或手动）
python manage.py setup
# 或手动：
# psql -U luckin -d luckin_spatial -f sql/schema.sql

# 6. （可选）从备份恢复完整数据
psql -U luckin -h localhost -d luckin_spatial -f data/luckin_spatial_backup.sql

# 7. 启动后端（在 luckin-spatial-analysis 目录下）
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
# 看到 "Uvicorn running on http://0.0.0.0:8000" 就说明成功了
# 打开 http://localhost:8000/docs 查看 API 文档

# 8. 另开一个终端，启动前端
cd frontend
npm install        # 首次运行需要
npm run dev
# 看到 "Local: http://localhost:3000" 就说明成功了
```

### 方法二：Docker 一键启动（推荐演示/部署）

```bash
docker compose up -d
# 前端: http://localhost:3000
# 后端: http://localhost:8000/docs
```

### 数据采集（如果需要完整数据）

按以下顺序运行脚本（每个脚本可能需要数小时，因为高德 API 有日调用限额）：

```bash
python scripts/fetch_stores.py     # 1. 采集瑞幸+星巴克门店
python scripts/fetch_pois.py       # 2. 采集 POI 数据
python scripts/import_osm.py       # 3. 导入 OSM 基础数据
python scripts/compute_features.py # 4. 计算空间关联特征
python scripts/train_model.py      # 5. 训练选址模型
```

---

## 10. 常见问题排查

### 后端启动报错：`ImportError: attempted relative import`

**原因**：从 `backend/` 目录内直接运行了 `uvicorn main:app`。

**解决**：回到 `luckin-spatial-analysis/` 目录，用模块方式启动：
```bash
cd luckin-spatial-analysis
python -m uvicorn backend.main:app --port 8000
```

### 后端启动报错：`[Errno 10048] address already in use`

**原因**：8000 端口被占用了。

**解决**：
```bash
# 换一个端口
python -m uvicorn backend.main:app --port 8001
```
前端需要同步修改：启动时加环境变量 `VITE_API_TARGET=http://localhost:8000 npm run dev`

### 前端启动报错：`npx: command not found` 或 `'npx' 不是内部或外部命令`

**原因**：Node.js 未安装或未添加到系统 PATH。

**解决**：
```bash
# 检查 Node.js 是否安装
node --version

# 如果未安装，下载安装 Node.js 20+
# https://nodejs.org/

# 如果已安装但无法识别，检查 PATH 环境变量
where node
# 将 Node.js 安装目录添加到系统 PATH 后重启终端
```

### 前端报错：`Network Error` 或 API 请求失败

**原因**：可能是后端没启动，或者端口不匹配。

**排查**：
1. 确认后端在运行：访问 `http://localhost:8000/docs` 看是否打开
2. 检查 `vite.config.ts` 里的 proxy target 端口是否和后端一致
3. 如果后端换了端口，前端启动时设置 `VITE_API_TARGET`

### 数据库连接失败

**原因**：PostgreSQL 没启动，或连接信息不对。

**排查**：
```bash
# 检查数据库是否在运行
pg_isready -h localhost -p 5432

# 如果用的 Docker
docker ps | grep postgis
```

### 地图上不显示底图

**原因**：默认用的是免费 CartoDB 底图，不需要 API Key。如果加载失败可能是网络问题。

**解决**：检查浏览器控制台是否有 Tile 加载错误，必要时换 Mapbox 底图（需要注册免费 Token）。

---

## 11. 如果想二次开发？

### 推荐的开发顺序

```
1. 先跑起来（不需要数据库也能看前端页面结构）
   └→ npm run dev

2. 读一遍后端 API，理解数据格式
   └→ 打开 http://localhost:8000/docs

3. 改一个简单功能练手
   └→ 比如在首页加一个统计指标

4. 然后去改数据采集或分析逻辑
   └→ scripts/ 下的 Python 文件
```

### 代码修改指南

| 想改什么 | 去改哪个文件 |
|---------|------------|
| 加一个新 API | `backend/routers/` 下新建或在现有文件里加路由 |
| 改前端页面布局 | `frontend/src/pages/` 下对应的 `.tsx` |
| 加一个新的 POI 类别 | `scripts/config.py` 的 `AMAP_TYPECODE_MAP` |
| 调整模型参数 | `scripts/train_model.py` 的 `XGBClassifier(...)` |
| 改数据库结构 | `sql/schema.sql`，然后重建 |
| 加新的分析指标 | `sql/analytics.sql` 写新查询 |

### 技术栈速查

| 层级 | 技术 | 学习资源 |
|------|------|---------|
| 前端 | React 18 + TypeScript + Ant Design | React 官方教程 |
| 地图 | deck.gl + MapLibre GL | deck.gl 官网 examples |
| 图表 | ECharts | ECharts 官方示例 |
| 后端 | FastAPI + Pydantic + asyncpg | FastAPI 官方教程 |
| 空间 | PostgreSQL + PostGIS | PostGIS 官方文档 |
| 分析 | GeoPandas + Shapely + osmnx | GeoPandas 入门 |
| 模型 | XGBoost + SHAP + scikit-learn | XGBoost 文档 |

---

> **文档版本**：v1.0 | **最后更新**：2026-07-22

---

### 快速参考卡片

```
一键启动:  python manage.py start
          或双击 启动服务.bat

启动后端:  python manage.py backend
          或 python -m uvicorn backend.main:app --port 8000

启动前端:  python manage.py frontend
          或 cd frontend && npm run dev

停止服务:  python manage.py stop
          或双击 停止服务.bat

管理控制台: python manage.py   (交互式菜单)

API 文档:  http://localhost:8000/docs
前端页面:  http://localhost:3000

数据库:    PostgreSQL 15 + PostGIS 3.6, 端口 5432
           docker compose up -d postgis (仅数据库)
           备份文件: data/luckin_spatial_backup.sql (34MB)

数据采集:  python scripts/fetch_stores.py  → 门店
           python scripts/fetch_pois.py    → POI
           python scripts/compute_features.py → 关联特征
           python scripts/train_model.py   → 模型

核心文件:  sql/schema.sql         ← 数据库结构
           backend/main.py        ← 后端入口
           frontend/src/App.tsx   ← 前端入口
           manage.py              ← 管理控制台
```
