# 🍵 瑞幸空间分析平台 — 依赖及安装文档

> 适用环境：Windows 10/11 | Python 3.11+ | Node.js 24+ | PostgreSQL 15

---

## 1. 核心依赖

### 1.1 数据库

| 组件 | 版本 | 说明 |
|------|------|------|
| PostgreSQL | 15.8 | 关系型数据库 |
| PostGIS | 3.6.2 | 空间扩展（几何类型、空间索引、距离计算） |

**Windows 安装：**

```powershell
# 1. 下载 PostgreSQL 15 安装包
# https://www.enterprisedb.com/downloads/postgres-postgresql-downloads
# 或直接下载：
# https://get.enterprisedb.com/postgresql/postgresql-15.8-1-windows-x64.exe
# 安装时设置超级用户密码，默认端口 5432

# 2. 下载 PostGIS 3.6 捆绑包
# https://download.osgeo.org/postgis/windows/pg15/postgis-bundle-pg15-3.6.2x64.zip
# 解压后复制文件到 PostgreSQL 15 安装目录：
#   bin/*.dll  → C:\Program Files\PostgreSQL\15\bin\
#   lib/*      → C:\Program Files\PostgreSQL\15\lib\
#   share/extension/* → C:\Program Files\PostgreSQL\15\share\extension\
```

**或使用 Docker（推荐，需要 WSL2）：**

```bash
docker run -d --name luckin-postgis \
  -e POSTGRES_DB=luckin_spatial \
  -e POSTGRES_USER=luckin \
  -e POSTGRES_PASSWORD=your_db_password_here \
  -p 5432:5432 \
  postgis/postgis:15-3.3
```

### 1.2 Python 3.11+

| 包 | 版本 | 用途 |
|----|------|------|
| fastapi | ≥0.109 | Web 框架 |
| uvicorn | ≥0.27 | ASGI 服务器 |
| asyncpg | ≥0.29 | 异步 PostgreSQL 驱动 |
| pydantic | ≥2.5 | 数据验证 |
| pydantic-settings | ≥2.0 | 配置管理 |
| aiohttp | ≥3.9 | 异步 HTTP 客户端（调用高德 API） |
| numpy | ≥1.26 | 数值计算 |
| scikit-learn | ≥1.3 | 机器学习（模型评估） |
| xgboost | ≥2.0 | 选址预测模型 |
| shap | ≥0.43 | 模型可解释性 |
| joblib | ≥1.3 | 模型序列化 |
| pandas | ≥2.1 | 数据处理 |
| osmnx | ≥1.7 | OpenStreetMap 数据导入 |

**安装：**

```bash
# API 运行时依赖
pip install -r requirements-api.txt

# 数据管道依赖
pip install -r requirements.txt
```

### 1.3 Node.js 24+

| 包 | 版本 | 用途 |
|----|------|------|
| react | ^18.2 | UI 框架 |
| typescript | ^5.3 | 类型检查 |
| vite | ^5.0 | 构建工具 |
| antd | ^5.12 | UI 组件库 |
| @deck.gl/core | ^9.0 | WebGL 地图渲染 |
| @deck.gl/layers | ^9.0 | 散点/热力图层 |
| @deck.gl/aggregation-layers | ^9.0 | 聚合图层 |
| @deck.gl/react | ^9.0 | React 集成 |
| react-map-gl | ^7.1 | MapLibre/Mapbox 集成 |
| maplibre-gl | ^4.0 | 开源地图引擎 |
| echarts | ^5.4 | 统计图表 |
| echarts-for-react | ^3.0 | React 集成 |
| axios | ^1.6 | HTTP 客户端 |
| react-router-dom | ^6.20 | 前端路由 |
| dayjs | ^1.11 | 日期处理 |

**安装：**

```bash
cd frontend
npm install
```

---

## 2. 外部服务

### 2.1 高德地图 API

注册并获取 Key：https://lbs.amap.com/

需要开通以下服务：
- Web服务 API — 地点搜索（POI 采集）
- Web服务 API — 逆地理编码（坐标→城市）

### 2.2 底图瓦片

使用高德地图瓦片（免费），在 `MapView.tsx` 中配置：
```
https://webrd0{1-4}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8
```

---

## 3. 环境变量

在项目根目录创建 `.env`：

```env
# 高德地图 API Key
AMAP_API_KEY=your_amap_key_here

# PostgreSQL 连接
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=luckin_spatial
POSTGRES_USER=luckin
POSTGRES_PASSWORD=your_db_password_here
```

---

## 4. 数据库备份恢复

### 4.1 从备份文件恢复数据库

项目提供完整的数据库备份文件 `data/luckin_spatial_backup.sql`（34MB），包含全部门店、POI、预计算缓存和物化视图数据。

```bash
# 1. 确保数据库已创建
psql -U postgres -h localhost -c "CREATE DATABASE luckin_spatial;"

# 2. 启用 PostGIS 扩展
psql -U postgres -h localhost -d luckin_spatial -c "CREATE EXTENSION postgis;"
psql -U postgres -h localhost -d luckin_spatial -c "CREATE EXTENSION postgis_topology;"

# 3. 恢复备份
psql -U luckin -h localhost -d luckin_spatial -f data/luckin_spatial_backup.sql
```

### 4.2 使用 manage.py 初始化数据库

`manage.py setup` 命令可自动完成数据库初始化（建表 + 扩展 + 导入备份）：

```bash
# 自动初始化（交互式确认）
python manage.py setup

# 该命令会:
# 1. 检查数据库连接
# 2. 创建 PostGIS 扩展
# 3. 执行 sql/schema.sql 建表
# 4. 检测到备份文件后提示是否导入
```

---

## 5. 依赖安装检查清单

- [ ] PostgreSQL 15 已安装并运行
- [ ] PostGIS 3.6 扩展已启用（`CREATE EXTENSION postgis;` 成功）
- [ ] Python 3.11+ 已安装，`pip install -r requirements.txt` 成功
- [ ] Node.js 24+ 已安装，`cd frontend && npm install` 成功
- [ ] 高德 API Key 已配置到 `.env`
- [ ] 数据库 `luckin_spatial` 已创建
- [ ] 数据库用户 `luckin` 已创建并有权限
