# 瑞幸咖啡门店选址与POI时空演变分析平台

## 项目方案文档

> **版本**：v1.0  
> **日期**：2026-07-18  
> **作者**：上海海洋大学（空间信息与数字技术 本科）/ 格拉斯哥大学（地理信息技术与制图学 硕士）  
> **项目周期**：3周（21个工作日）

---

## 目录

1. [项目概述](#一项目概述)
2. [背景与问题](#二背景与问题)
3. [项目目标](#三项目目标)
4. [技术架构](#四技术架构)
5. [数据来源与采集方案](#五数据来源与采集方案)
6. [数据库设计](#六数据库设计)
7. [分析模块设计](#七分析模块设计)
8. [后端API设计](#八后端api设计)
9. [前端可视化设计](#九前端可视化设计)
10. [机器学习模型设计](#十机器学习模型设计)
11. [实施计划](#十一实施计划)
12. [预期成果与交付物](#十二预期成果与交付物)
13. [风险评估与应对](#十三风险评估与应对)
14. [技术栈选型理由](#十四技术栈选型理由)
15. [创新点与竞争优势](#十五创新点与竞争优势)

---

## 一、项目概述

### 1.1 项目名称

**瑞幸咖啡门店选址与POI时空演变分析平台**（Luckin Coffee Store Location & POI Spatio-temporal Evolution Analysis Platform）

### 1.2 一句话描述

基于 GIS 空间分析、机器学习和 Web 可视化技术，对瑞幸咖啡全国 ~22,000 家门店的时空扩张轨迹、POI 选址规律、与星巴克的差异化竞争策略进行系统性量化研究，并训练选址推荐模型。

### 1.3 项目定位

| 维度 | 定位 |
|------|------|
| **学术价值** | 商业地理学 + 空间数据科学交叉研究，可发表 GIS/ urban planning 方向论文 |
| **商业价值** | 选址策略量化分析，对新零售企业选址决策具有参考意义 |
| **求职价值** | 全栈 GIS 项目展示，覆盖数据采集→空间分析→ML建模→Web可视化的完整链路 |

---

## 二、背景与问题

### 2.1 行业背景

中国咖啡市场在过去十年经历了爆发式增长。瑞幸咖啡自2017年10月在北京银河SOHO开设首家门店以来，以"自提+外卖"的快取店模式迅速扩张，2023年门店数突破10,000家，2024年突破20,000家，成为全球门店数量最多的咖啡连锁品牌。瑞幸的选址策略——"下沉市场 + 数字化驱动 + 快取店模式"——与星巴克传统"第三空间"策略形成鲜明对比，成为商业地理学和零售选址研究的经典案例。

### 2.2 核心问题

| # | 研究问题 | 研究方法 |
|---|---------|---------|
| Q1 | 瑞幸门店的空间扩张轨迹呈现出怎样的规律？是否存在从一线城市向低线城市"下沉"的显著趋势？ | 核密度估计(KDE)、标准差椭圆(SDE)、城市渗透指数 |
| Q2 | 瑞幸门店选址与周边POI（写字楼、地铁站、商场、学校等）之间存在怎样的关联？哪些POI类型对选址影响最大？ | 缓冲区统计、Lift值分析、K-Means聚类 |
| Q3 | 瑞幸与星巴克的选址策略有何差异？瑞幸的"贴身战术"（开在星巴克附近）有多普遍？ | 最近邻距离分析、共址率计算、POI偏好对比 |
| Q4 | 能否基于POI特征训练一个选址推荐模型，为潜在新店位置评分？ | XGBoost二分类、SHAP可解释性分析 |

### 2.3 技术挑战

- **数据规模**：全国 ~22,000 瑞幸门店 + ~7,000 星巴克门店 + 百万级 POI 数据，需要高效的数据库和前后端性能
- **历史数据缺失**：门店精确开业日期不公开，需要通过季度财报和工商信息反推
- **POI覆盖不均**：高德API有日调用限额，需结合OSM开放数据做全国覆盖
- **前端性能**：22,000个点位在浏览器中流畅渲染，需要GPU加速方案

---

## 三、项目目标

### 3.1 功能目标

| 模块 | 功能 | 产出 |
|------|------|------|
| 🔍 数据采集 | 瑞幸/星巴克门店 + 7大类POI + 行政边界 | PostgreSQL空间数据库 |
| 📊 时空演变 | 2017-2026门店扩张轨迹，城市下沉指数 | 时间轴地图 + 统计图表 |
| 🎯 POI关联 | 门店选址与POI类型关联分析，Lift值 | 雷达图 + Lift柱状图 |
| ⚔️ 竞品对比 | 瑞幸 vs 星巴克选址策略差异 | 双品牌对比可视化 |
| 🤖 选址预测 | XGBoost选址评分 + SHAP可解释性 | 交互式选址评分工具 |

### 3.2 技术目标

| 指标 | 目标值 |
|------|--------|
| 数据库查询响应 | 90% 的 API 请求 < 200ms |
| 模型 AUC | > 0.85 |
| 前端帧率 | 13K 点位渲染 > 30fps |
| 部署便利性 | Docker Compose 一键启动 |

### 3.3 学习目标

- 掌握 PostGIS 空间查询优化（GiST索引、ST_DWithin、物化视图）
- 实践 deck.gl GPU 渲染海量地理数据
- 理解 XGBoost + SHAP 在选址决策中的可解释性应用
- 建立从数据采集到产品交付的完整 GIS 工程能力

---

## 四、技术架构

### 4.1 架构全景图

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 (React 18)                         │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────┐  │
│  │  Map View   │  │  Timeline    │  │  Charts Dashboard  │  │
│  │  deck.gl    │  │  Slider      │  │  ECharts + AntD    │  │
│  │  Mapbox GL  │  │              │  │                    │  │
│  └──────┬──────┘  └──────┬───────┘  └─────────┬──────────┘  │
│         │                │                     │             │
│         └────────────────┼─────────────────────┘             │
│                          │ HTTP/REST                        │
├──────────────────────────┼──────────────────────────────────┤
│                   后端 API (FastAPI)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ /api/stores  │  │ /api/analysis│  │ /api/prediction  │   │
│  │  门店CRUD     │  │  空间分析     │  │  选址评分+热力图   │   │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘   │
│         │                 │                    │             │
│         └─────────────────┼────────────────────┘             │
│                           │ asyncpg                         │
├───────────────────────────┼──────────────────────────────────┤
│              空间数据库 (PostgreSQL 15 + PostGIS 3.3)         │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐             │
│  │  stores  │  │   pois   │  │ admin_bounds  │             │
│  │ ~13K条   │  │ ~32K条   │  │ 全国省市县     │             │
│  └──────────┘  └──────────┘  └───────────────┘             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │store_poi_stats│  │  kde_grid    │  │ prediction_grid  │   │
│  │ 预计算缓存     │  │ 核密度网格    │  │ 预测网格          │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│                    数据采集层 (Python)                         │
│  ┌────────────┐  ┌────────────┐  ┌─────────────┐            │
│  │ 高德API    │  │ OSM/osmnx  │  │ 财报/工商    │            │
│  │ POI+门店   │  │ 道路/建筑   │  │ 开业时间     │            │
│  └────────────┘  └────────────┘  └─────────────┘            │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 数据流

```
[高德API] ──→ fetch_stores.py ──→ stores 表
[高德API] ──→ fetch_pois.py  ──→ pois 表
[OSM]     ──→ import_osm.py  ──→ pois 表（补充）
                                     │
                          compute_features.py
                                     │
                    ┌────────────────┼────────────────┐
                    ↓                ↓                ↓
            store_poi_stats    kde_grid      city_tiers
                    │
            train_model.py
                    │
          XGBoost 模型 (joblib)
                    │
            ┌───────┴───────┐
            ↓               ↓
    FastAPI 后端     React 前端
```

---

## 五、数据来源与采集方案

### 5.1 门店数据

| 来源 | 数据类型 | 说明 |
|------|---------|------|
| **高德地图 POI API** | 当前门店位置 | 按城市网格搜索，获取瑞幸+星巴克 21城门店坐标 |
| **瑞幸季度财报** | 历史门店数量 | 财报披露各季度末城市门店总数，用于反推开业时间 |
| **企查查/天眼查** | 工商注册信息 | 补充个体门店的注册时间作为开业日期的近似 |

**采集策略：**

```
高德 API 参数:
  - keywords: "瑞幸咖啡" / "星巴克"
  - citylimit: true （限制在目标城市）
  - extensions: all  （返回详细信息）
  - 分页: 每页25条，遍历到返回空

去重逻辑:
  - 主键: (名称前20字符 + 经纬度保留5位小数)
  - 距离 < 50m 视为同一门店
```

**实际采集数据量：**

| 品牌 | 全国门店数（2026估算） | 21城实际采集量 |
|------|----------------------|-----------|
| 瑞幸咖啡 | ~22,000 | 8,640 |
| 星巴克 | ~7,000 | 4,366 |

> 注意：高德 API 单城市最多返回 900 条记录（36页×25条），实际采集量受 API 限制，全国总数来自财报数据。

### 5.2 POI 数据

| 来源 | POI类型 | 覆盖范围 | 说明 |
|------|---------|---------|------|
| **OSM** | 道路网络、地铁站、公交站、建筑物轮廓 | 全国 | 开源免费，数据完整 |
| **高德 API** | 写字楼、购物中心、餐饮、便利店、学校 | 21个重点城市 | 商业POI精度高（实际采集 ~32,468条） |

**POI 类别体系（7大类）：**

| 大类 | 代码 | 子类举例 | 高德typecode |
|------|------|---------|-------------|
| 办公 | `office` | 写字楼、产业园、商务中心 | 120200-120203 |
| 商业 | `mall` | 购物中心、百货、商业街 | 060100-060400 |
| 交通 | `metro` / `bus` | 地铁站、公交站 | 150500-150800 |
| 教育 | `university` / `school` | 大学、中小学 | 141000-141400 |
| 居住 | `residential` | 住宅小区、公寓 | 120300-120304 |
| 餐饮 | `restaurant` / `cafe` / `tea` | 餐厅、咖啡厅、茶饮 | 050000-050500 |
| 生活 | `convenience` / `supermarket` / `bank` / `hospital` | 便利店、超市、银行、医院 | 060200-090100 |

### 5.3 重点城市选择（21个）

| 层级 | 城市 |
|------|------|
| **一线** (4) | 北京、上海、广州、深圳 |
| **新一线** (10) | 成都、杭州、重庆、武汉、西安、苏州、南京、长沙、郑州、天津 |
| **二线** (7) | 合肥、福州、厦门、昆明、沈阳、青岛、济南 |

> 这21个城市覆盖了瑞幸约80%的门店，POI数据由高德API精细采集（约32,468条）；其余城市以OSM数据做基础覆盖。

---

## 六、数据库设计

### 6.1 核心表结构

#### stores（门店表）

```sql
CREATE TABLE stores (
    id          SERIAL PRIMARY KEY,
    name        VARCHAR(200) NOT NULL,        -- 门店名称
    brand       VARCHAR(50) NOT NULL,          -- 'luckin' | 'starbucks'
    address     VARCHAR(500),
    lng         DOUBLE PRECISION NOT NULL,
    lat         DOUBLE PRECISION NOT NULL,
    geom        GEOMETRY(POINT, 4326) NOT NULL,
    city        VARCHAR(100),
    district    VARCHAR(100),
    province    VARCHAR(100),
    adcode      VARCHAR(20),
    open_date   DATE,                          -- 开业日期（部分为估算）
    store_type  VARCHAR(50),                   -- 快取店/悠享店/外卖厨房/旗舰店
    status      VARCHAR(20) DEFAULT '营业中',
    confidence  DOUBLE PRECISION DEFAULT 1.0,  -- 开业日期置信度
    data_source VARCHAR(50) DEFAULT 'amap',
    created_at  TIMESTAMP DEFAULT NOW(),
    updated_at  TIMESTAMP DEFAULT NOW()
);

-- 空间索引（核心性能保障）
CREATE INDEX idx_stores_geom ON stores USING GIST(geom);
CREATE INDEX idx_stores_brand ON stores(brand);
CREATE INDEX idx_stores_city ON stores(city);
CREATE INDEX idx_stores_open_date ON stores(open_date);
CREATE INDEX idx_stores_brand_city ON stores(brand, city);
```

#### pois（POI表）

```sql
CREATE TABLE pois (
    id            SERIAL PRIMARY KEY,
    name          VARCHAR(200) NOT NULL,
    category      VARCHAR(50) NOT NULL,        -- POI大类
    sub_category  VARCHAR(100),
    lng           DOUBLE PRECISION NOT NULL,
    lat           DOUBLE PRECISION NOT NULL,
    geom          GEOMETRY(POINT, 4326) NOT NULL,
    address       VARCHAR(500),
    city          VARCHAR(100),
    district      VARCHAR(100),
    adcode        VARCHAR(20),
    data_source   VARCHAR(50) DEFAULT 'amap',   -- amap | osm
    amap_typecode VARCHAR(20),
    created_at    TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_pois_geom ON pois USING GIST(geom);
CREATE INDEX idx_pois_category ON pois(category);
CREATE INDEX idx_pois_city ON pois(city);
CREATE INDEX idx_pois_category_city ON pois(category, city);
```

#### store_poi_stats（门店-POI关联缓存表）

```sql
CREATE TABLE store_poi_stats (
    store_id     INT REFERENCES stores(id) ON DELETE CASCADE,
    radius       INT NOT NULL CHECK (radius IN (200, 500, 1000)),
    poi_category VARCHAR(50) NOT NULL,
    poi_count    INT NOT NULL DEFAULT 0,
    last_updated TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY (store_id, radius, poi_category)
);
```

> **设计理念**：预计算三个半径（200m/500m/1000m）的POI统计并缓存到该表，避免每次API请求时重复执行 `ST_DWithin`。数据更新时通过 `refresh_store_poi_stats()` 函数批量重建。

### 6.2 辅助表

| 表名 | 用途 | 数据量 |
|------|------|--------|
| `admin_boundaries` | 全国省市县行政边界（GeoJSON/MultiPolygon） | ~3,000条 |
| `city_tiers` | 城市层级 + 人口 + GDP | 21条 |
| `kde_grid` | 核密度分析网格缓存 | 按需生成 |
| `prediction_grid` | 选址预测网格缓存 | 按需生成 |
| `random_points` | 随机采样点（Lift值计算的对照组） | ~20,000条 |
| `mv_city_stats` | 城市级统计物化视图（定时刷新） | ~40条 |

### 6.3 性能优化策略

| 策略 | 实现 | 预期效果 |
|------|------|---------|
| GiST 空间索引 | `CREATE INDEX ... USING GIST(geom)` | 空间查询从O(n)降为O(log n) |
| 预计算缓存 | `store_poi_stats` 表 | API响应从秒级降为毫秒级 |
| 物化视图 | `mv_city_stats` | 城市统计页面零延迟 |
| 连接池 | asyncpg connection pool | 支持并发请求 |
| 批量写入 | 500条/事务 | 数据导入效率提升10x |

---

## 七、分析模块设计

### 模块一：时空演变轨迹

**研究问题**：瑞幸门店从2017年至今的空间扩张轨迹呈现出怎样的规律？

**分析方法**：

| 方法 | 说明 | 输出 |
|------|------|------|
| **核密度估计 (KDE)** | 按年份计算门店空间密度，生成热力图序列 | GeoJSON栅格 |
| **标准差椭圆 (SDE)** | 逐年计算门店分布的中心点、方向角和离散程度 | 椭圆参数 |
| **城市下沉指数** | `Urban Penetration Index = 二线以下城市门店占比 / 总门店数` | 时间序列 |
| **Moran's I** | 检验门店空间分布是否显著聚集（全局空间自相关） | 统计值 + p-value |

**前端交互设计：**

```
┌──────────────────────────────────────────────────┐
│  [时间轴滑块: 2017 ■━━━━━━━━━● 2026]              │
│                                                    │
│  ┌─────────────────────┐  ┌──────────────────────┐│
│  │                     │  │  2024年关键指标        ││
│  │   全国热力图地图      │  │  📍 累计门店: 18,234  ││
│  │   (deck.gl)         │  │  🏙️ 覆盖城市: 320+    ││
│  │                     │  │  📈 城市下沉指数: 42%  ││
│  │                     │  │  🏢 快取店占比: 71%    ││
│  └─────────────────────┘  └──────────────────────┘│
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │  月度新增门店 (柱状图) + 累计门店 (折线)        │  │
│  │  ██▆▃▁  ▁▃▆██  ██▆▃▁                         │  │
│  └──────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 模块二：POI关联分析

**研究问题**：瑞幸门店选址与周边POI之间存在怎样的关联规律？

**分析方法**：

**① 缓冲区统计**
以每个门店为中心，计算200m/500m/1000m半径内各类POI数量，汇总统计均值、标准差、分布。

**② Lift值分析**

```
Lift(category) = P(POI_near_store | category) / P(POI_random | category)

Lift > 1.5 → 该POI在门店周边显著富集（选址吸引因素）
Lift ≈ 1.0 → 与随机分布无显著差异
Lift < 0.8 → 门店倾向于避开该POI类型
```

**③ 选址类型聚类 (K-Means)**

基于POI特征向量对门店进行K-Means聚类（K=4），识别四种选址类型：

| 聚类 | 命名 | POI特征 | 占比（预估） |
|------|------|---------|------------|
| Cluster 0 | **写字楼型** | office↑, metro↑, cafe↑ | ~45% |
| Cluster 1 | **商圈型** | mall↑, restaurant↑, convenience↑ | ~25% |
| Cluster 2 | **社区型** | residential↑, school↑, supermarket↑ | ~20% |
| Cluster 3 | **交通枢纽型** | metro↑↑, bus↑↑ | ~10% |

**前端交互设计：**

```
┌──────────────────────┐  ┌──────────────────────┐
│   POI 雷达图          │  │   POI Lift 值         │
│   (各POI类别均值)      │  │   (富集程度排序)       │
│                      │  │                      │
│       metro          │  │  office  ████████ 2.3│
│      /     \         │  │  metro   ███████  1.8│
│   bus       office   │  │  mall    █████    1.5│
│      \     /         │  │  cafe    ████     1.2│
│     residential      │  │  ...                 │
│                      │  │  ------ 基准线 ------  │
└──────────────────────┘  └──────────────────────┘
```

### 模块三：瑞幸 vs 星巴克对比

**研究问题**：两个品牌的选址策略有何本质差异？

**分析维度**：

| 维度 | 方法 | 预期发现 |
|------|------|---------|
| **城市策略** | 按城市层级统计门店占比 | 瑞幸在二三线城市占比更高 |
| **最近邻距离** | 每个瑞幸门店到最近星巴克的距离 | 中位数 ~500m-800m |
| **共址率** | 200m内存在星巴克的瑞幸门店占比 | 预估 ~15-20% |
| **POI偏好差异** | 双品牌Lift值对比矩阵 | 瑞幸更依赖办公POI，星巴克更依赖商业POI |

### 模块四：选址预测模型

详见[第十章](#十机器学习模型设计)。

---

## 八、后端API设计

### 8.1 API 概览

| 方法 | 路径 | 说明 | 参数 |
|------|------|------|------|
| `GET` | `/api/stores` | 门店列表（分页+筛选） | brand, city, open_date_from/to, limit, offset |
| `GET` | `/api/stores/{id}` | 门店详情+周边POI统计 | — |
| `GET` | `/api/stores/heatmap/data` | 热力图GeoJSON | brand, city, year |
| `GET` | `/api/stores/timeline/data` | 时间轴聚合数据 | brand, city |
| `GET` | `/api/stores/cities/list` | 城市列表+门店数 | — |
| `GET` | `/api/analysis/store-poi-correlation` | POI关联统计 | brand, radius, city |
| `GET` | `/api/analysis/lift-scores` | POI Lift值 | brand, radius |
| `GET` | `/api/analysis/brand-comparison` | 品牌对比 | city |
| `GET` | `/api/analysis/city-tiers-stats` | 城市层级统计 | — |
| `POST` | `/api/prediction/score` | 选址评分 | `{lng, lat, city}` |
| `GET` | `/api/prediction/heatmap` | 城市预测热力图 | city, grid_size_m |

### 8.2 响应示例

**`POST /api/prediction/score` 响应：**

```json
{
  "score": 0.82,
  "lng": 121.4737,
  "lat": 31.2304,
  "city": "上海",
  "features": {
    "metro_500": 3,
    "office_500": 12,
    "mall_500": 2,
    "restaurant_500": 45,
    "cafe_500": 8,
    "residential_500": 15,
    "university_500": 1
  },
  "top_positive": [
    {"feature": "office_500", "value": 12, "importance": 0.28},
    {"feature": "metro_500", "value": 3, "importance": 0.18},
    {"feature": "restaurant_500", "value": 45, "importance": 0.12}
  ],
  "top_negative": [],
  "model_available": true
}
```

### 8.3 性能目标

| 端点 | 目标响应时间 | 优化手段 |
|------|------------|---------|
| `/api/stores` | < 100ms | GiST索引 + LIMIT分页 |
| `/api/stores/heatmap/data` | < 500ms | 预计算 + 空间索引 |
| `/api/analysis/store-poi-correlation` | < 200ms | `store_poi_stats`缓存表 |
| `/api/prediction/score` | < 300ms | 预加载模型 + POI计数走索引 |

---

## 九、前端可视化设计

### 9.1 页面结构

```
┌─────────────────────────────────────────────┐
│  🍵 瑞幸空间分析   [总览] [时空演变] [POI分析]  │  ← Ant Design 导航
│                           [竞品对比] [选址预测] │
├─────────────────────────────────────────────┤
│                                              │
│              页面内容区                        │
│                                              │
└─────────────────────────────────────────────┘
```

### 9.2 各页面设计

#### 首页 — 总览仪表盘

```
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 瑞幸门店  │ │ 星巴克    │ │ 覆盖城市  │ │ 年均增长  │
│ 21,847   │ │  6,892   │ │   328    │ │   42%    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘

┌─────────────────────────────────────────────┐
│          全国门店分布总览                      │
│  ┌───────────────────────────────────────┐  │
│  │                                       │  │
│  │      deck.gl ScatterplotLayer         │  │
│  │      22K 散点 (蓝色=瑞幸 绿色=星巴克)    │  │
│  │                                       │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

#### 时空演变页

- **地图层**：deck.gl `HeatmapLayer`，按年份切换
- **时间轴**：Ant Design `Slider`，2017-2026 逐年级
- **图表**：ECharts 堆叠面积图（月度新增 + 累计门店）
- **指标面板**：当前年份的关键数字

#### POI分析页

- **雷达图**：ECharts 雷达图展示各类POI在门店周边的平均密度
- **Lift柱状图**：ECharts 柱状图，红色基准线标记 Lift=1.0
- **详情表格**：Ant Design `Table` 展示原始统计数据

#### 竞品对比页

- **城市层级柱状图**：ECharts 分组柱状图
- **指标对比表格**：关键维度对比
- **策略解读卡片**：文字总结

#### 选址预测页

- **地图点选**：点击地图任意位置 → 自动填充坐标 → 评分
- **评分结果**：大数字 + 颜色编码（绿/黄/红）
- **特征重要性**：ECharts 横向柱状图
- **模型说明**：模型类型、特征体系、使用建议

### 9.3 地图渲染性能方案

| 场景 | 图层类型 | 数据量 | 策略 |
|------|---------|--------|------|
| 全国总览 | `ScatterplotLayer` | ~13K点 | deck.gl GPU实例化渲染 |
| 热力图 | `HeatmapLayer` | ~13K点 | GPU着色器聚合 |
| 城市柱状图 | `ColumnLayer` | ~40柱 | 3D柱状图，按城市聚合 |
| 预测网格 | `PolygonLayer` | ~500格 | 按缩放级别动态加载 |

### 9.4 技术选型

| 组件 | 技术 | 理由 |
|------|------|------|
| UI框架 | Ant Design 5.x | 适合数据仪表盘场景，组件丰富 |
| 地图渲染 | deck.gl 9.x | GPU加速，原生支持22K+点位流畅渲染 |
| 底图 | CartoDB Positron (免费) / Mapbox | 浅色底图，不抢数据焦点 |
| 统计图表 | ECharts 5.x | 地图以外的所有图表需求 |
| 路由 | React Router 6.x | SPA页面导航 |
| HTTP | Axios | 请求拦截 + 错误处理 |

---

## 十、机器学习模型设计

### 10.1 问题定义

**任务类型**：二分类  
**正样本**：瑞幸现有门店位置（约18,000个，来自20个重点城市）  
**负样本**：同城市随机采样位置（约18,000个，1:1 平衡）  
**目标**：预测一个位置是否适合开设瑞幸门店

### 10.2 特征工程

每个样本点提取 **27个特征**（7类POI × 3个半径 + 城市层级 × 3个半径）：

| 特征组 | 特征名 | 说明 |
|--------|--------|------|
| 交通 | `metro_200/500/1000` | 半径内地铁站数量 |
| 交通 | `bus_200/500/1000` | 半径内公交站数量 |
| 办公 | `office_200/500/1000` | 半径内写字楼数量 |
| 商业 | `mall_200/500/1000` | 半径内购物中心数量 |
| 餐饮 | `restaurant_200/500/1000` | 半径内餐厅数量 |
| 餐饮 | `cafe_200/500/1000` | 半径内咖啡厅数量 |
| 居住 | `residential_200/500/1000` | 半径内住宅小区数量 |
| 教育 | `university_200/500/1000` | 半径内大学数量 |
| 生活 | `convenience_200/500/1000` | 半径内便利店数量 |
| 城市 | `city_tier` | 城市层级（1-4） |

**为什么选三个半径尺度？**
- **200m**：步行1-2分钟，对应门店的"即时可达"范围
- **500m**：步行5-7分钟，瑞幸用户愿意自提的典型距离
- **1000m**：步行10-15分钟，外卖覆盖的核心范围

### 10.3 模型选择：XGBoost

| 对比维度 | XGBoost | 随机森林 | 逻辑回归 | LightGBM |
|---------|---------|---------|---------|----------|
| 非线性关系 | ✅ 强 | ✅ 强 | ❌ 弱 | ✅ 强 |
| 特征交互 | ✅ 自动 | ✅ 自动 | ❌ 手动 | ✅ 自动 |
| 可解释性 | ⭐⭐⭐⭐ (SHAP) | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 训练速度 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 小数据表现 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |

**选择理由**：XGBoost 在表格数据上的综合表现最优，且 SHAP 支持提供了选址决策所需的可解释性。

**超参数配置**：

```python
XGBClassifier(
    n_estimators=200,       # 树的数量
    max_depth=6,            # 单棵树最大深度
    learning_rate=0.05,     # 学习率
    subsample=0.8,          # 行采样
    colsample_bytree=0.8,   # 列采样
    random_state=42,
    eval_metric='auc',
    early_stopping_rounds=20,
)
```

### 10.4 评估指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| AUC-ROC | > 0.85 | 区分门店位置和随机位置的能力 |
| Precision | > 0.80 | 推荐位置中真正适合的比例 |
| Recall | > 0.80 | 现有门店中被模型认可的比例 |
| Cross-validation | 5-fold std < 0.03 | 模型稳定性 |

### 10.5 可解释性：SHAP

使用 SHAP (SHapley Additive exPlanations) 解释每个位置的预测：

- **全局解释**：哪些特征对选址最重要（SHAP summary plot）
- **局部解释**：单个位置为什么评分为0.82（SHAP force plot）
- **前端展示**：选址评分页面显示Top-5正面/负面特征贡献

---

## 十一、实施计划

### 11.1 总体时间线

```
Week 1            Week 2            Week 3
数据采集           数据处理与建模       前后端开发与部署
│──┬──┬──┬──┬──│──┬──┬──┬──┬──│──┬──┬──┬──┬──│
D1 D2 D3 D4 D5  D8 D9 D10D11D12 D15D16D17D18D19
   门店  POI   OSM    空间   分析    后端  前端   部署
   采集  采集   导入   关联   建模    API   页面   验证
```

### 11.2 详细计划

#### 第1周：数据采集

| 天数 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| D1 | 高德API Key申请，环境搭建，数据库初始化 | PostgreSQL运行，schema执行 | — |
| D2 | 瑞幸门店数据采集（21城） | 8,640条门店 | D1 |
| D3 | 星巴克门店数据采集（21城） | 4,366条门店 | D1 |
| D4 | OSM全国路网/地铁/建筑数据导入 | 基础POI数据 | D1 |
| D5 | 高德API 21城POI精细采集 | ~32,468条POI | D1 |
| D6 | POI数据补充 + 去重清洗 | 干净POI数据 | D4,D5 |
| D7 | 数据验证：门店数与财报比对 | 数据质量报告 | D2,D3 |

#### 第2周：数据处理与分析

| 天数 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| D8 | 空间关联计算：`store_poi_stats` 表填充 | 预计算缓存 | W1 |
| D9 | 城市层级数据 + 物化视图 | `city_tiers`, `mv_city_stats` | W1 |
| D10 | KDE + SDE 时空分析 | 分析结果 | D8 |
| D11 | POI Lift值 + K-Means 聚类 | 分析结果 | D8 |
| D12 | 瑞幸 vs 星巴克对比分析 | 对比报告 | D8 |
| D13 | XGBoost 模型训练 + SHAP 分析 | 模型文件 | D8 |
| D14 | 模型评估 + 交叉验证 | AUC > 0.85 验证 | D13 |

#### 第3周：前后端开发与部署

| 天数 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| D15 | FastAPI 后端开发（stores路由） | 门店API | W1,W2 |
| D16 | FastAPI 后端开发（analysis + prediction路由） | 全API可用 | D15 |
| D17 | React 前端搭建 + 首页/时空演变页 | 2个页面 | D16 |
| D18 | POI分析页 + 竞品对比页 | 2个页面 | D16 |
| D19 | 选址预测页 | 1个页面 | D16 |
| D20 | 全链路联调 + 性能优化 | Bug修复 | D17-D19 |
| D21 | Docker Compose 部署 + README + Demo录制 | 可演示的完整项目 | D20 |

### 11.3 里程碑

| 里程碑 | 日期 | 验收标准 |
|--------|------|---------|
| M1: 数据就绪 | D7 | 数据库含 ~13K 门店 + ~32K POI，门店数与财报误差合理 |
| M2: 分析完成 | D14 | 四个分析模块产出结果，模型 AUC > 0.85 |
| M3: 系统上线 | D21 | Docker Compose一键启动，5个页面功能正常 |

---

## 十二、预期成果与交付物

### 12.1 代码交付物

| 交付物 | 说明 |
|--------|------|
| GitHub 仓库 | 包含全部源代码的公开仓库 |
| Docker 镜像 | 一键部署的容器化版本 |
| API 文档 | FastAPI 自动生成的 Swagger 文档 |
| **管理控制台** | `manage.py`（~600行），支持 start/stop/restart/status/backend/frontend/setup/logs/open 命令，交互式菜单，附带 Windows .bat 快捷启动文件 |
| **数据库备份** | `data/luckin_spatial_backup.sql`（34MB），全量数据一键恢复 |

### 12.2 数据交付物

| 交付物 | 格式 | 说明 |
|--------|------|------|
| 门店数据集 | CSV + GeoJSON | 21城瑞幸+星巴克门店（脱敏版本） |
| POI关联统计 | CSV | 每家门店周边POI密度矩阵 |
| 选址模型 | .pkl (joblib) | 训练好的XGBoost模型 |

### 12.3 分析报告

| 报告 | 形式 | 内容 |
|------|------|------|
| 数据质量报告 | Markdown | 数据采集范围、覆盖率、与财报比对结果 |
| 空间分析报告 | Jupyter Notebook × 4 | 数据探索、时空分析、POI关联、选址预测 |
| 项目总结报告 | Markdown | 核心发现、技术总结、改进方向 |

### 12.4 可视化产出

| 产出 | 形式 | 说明 |
|------|------|------|
| 交互式Web应用 | Docker部署 | 5页面完整功能 |
| Demo视频 | MP4 (3-5分钟) | 全功能演示，适合面试展示 |
| 静态图表 | PNG | 关键分析图表，可放入论文或汇报PPT |

### 12.5 核心分析发现（预期）

1. **扩张轨迹**：瑞幸从一线城市核心商圈起步，2019年起向新一线城市快速扩张，2022年后加速向二三线"下沉"
2. **核心选址逻辑**：半径500m内的写字楼数量（`office_500`）是选址的最强预测因子（特征重要性 ~28%），其次是地铁站密度（`metro_500`，~18%）
3. **选址类型分布**：写字楼型（~45%）> 商圈型（~25%）> 社区型（~20%）> 交通枢纽型（~10%）
4. **与星巴克差异**：瑞幸门店到最近星巴克的中位距离约600m，约18%的瑞幸门店开在星巴克200m以内（"贴身战术"）。在POI偏好上，瑞幸对办公类POI的依赖度（Lift 2.3）显著高于星巴克（Lift 1.6）
5. **模型性能**：XGBoost AUC 达到0.85+，Top-3重要特征为office_500、metro_500、mall_500

---

## 十三、风险评估与应对

| 风险项 | 概率 | 影响 | 应对措施 |
|--------|------|------|---------|
| 高德API日调用限额（5000次/日） | 高 | 数据采集延期 | 分多日采集，利用网格搜索减少调用次数；OSM补充基础POI |
| 门店历史开业日期缺失 | 高 | 时间分析精度降低 | 用财报季度城市门店数做近似；工商注册数据补充 |
| OSM建筑数据覆盖不完整 | 中 | 部分城市POI稀疏 | 明确标注"重点城市精细分析+全国宏观概览"的分层策略 |
| 13K点位前端卡顿 | 中 | 用户体验差 | deck.gl GPU渲染 + 按缩放级别动态聚合 |
| 选址模型过拟合 | 中 | 预测不准 | 负样本同城市随机采样 + 5-fold交叉验证 + SHAP验证合理性 |
| POI数据21城覆盖不均衡 | 低 | 部分城市分析精度低 | 前端明确标注"数据覆盖范围" |
| XGBoost训练时间过长 | 低 | 模型迭代慢 | 控制特征数量（27个），early_stopping提前终止 |

---

## 十四、技术栈选型理由

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| **数据采集** | Python (requests, aiohttp, osmnx) | 生态成熟，aiohttp异步提升API采集效率3-5倍 |
| **空间数据库** | PostgreSQL 15 + PostGIS 3.3 | GIS领域黄金标准，ST_DWithin/ST_ClusterDBSCAN/物化视图完善 |
| **空间分析** | GeoPandas + Shapely + PySAL | Python生态链完整，从数据清洗到统计检验一站式 |
| **后端框架** | FastAPI | 异步高性能，自动Swagger文档，类型安全，部署简单 |
| **前端框架** | React 18 + TypeScript | 生态最大，deck.gl React绑定成熟，TypeScript减少运行时错误 |
| **地图渲染** | deck.gl 9.x | GPU着色器加速，原生支持22K+点位的ScreenGridLayer/HeatmapLayer |
| **底图服务** | CartoDB Positron（免费） | 无需API Key，浅色主题不抢数据焦点 |
| **统计图表** | ECharts 5.x | 国产最强，中文友好，地图+图表全能 |
| **UI组件** | Ant Design 5.x | 数据仪表盘场景最佳实践，组件丰富 |
| **机器学习** | XGBoost + SHAP | 表格数据SOTA，SHAP提供业界标准的可解释性 |
| **部署** | Docker Compose | 一键启动3个服务（PostGIS + Backend + Frontend），面试演示零门槛 |

---

## 十五、创新点与竞争优势

### 15.1 技术创新

1. **三层半径特征体系**：200m/500m/1000m 三级缓冲区，覆盖步行可达、自提辐射、外卖辐射三个空间尺度，比单一半径分析更全面地刻画选址逻辑
2. **Lift值对比分析**：引入推荐系统中的Lift概念量化POI富集程度，比传统的简单计数更有统计显著性
3. **选址类型自动聚类**：K-Means将门店分为写字楼型/商圈型/社区型/交通枢纽型，提供选址策略的可解释分类
4. **SHAP可解释预测**：不仅给出评分，还解释为什么（哪些POI特征贡献最大），满足实际选址决策的透明性需求

### 15.2 工程创新

1. **预计算缓存架构**：`store_poi_stats` 表将空间关联计算从秒级降为毫秒级，Web应用体验媲美静态数据
2. **GPU前端渲染**：deck.gl代替传统Leaflet/OpenLayers，22K点位渲染帧率从 < 10fps 提升到 > 30fps
3. **全容器化部署**：`docker compose up -d` 一条命令启动完整系统

### 15.3 求职竞争力

| 能力维度 | 本项目证明 |
|---------|-----------|
| **GIS空间分析** | PostGIS空间查询、KDE、SDE、Moran's I、缓冲区统计 |
| **机器学习** | XGBoost训练、特征工程、SHAP可解释性、交叉验证 |
| **全栈工程** | FastAPI + React + deck.gl + Docker，从数据到产品 |
| **数据工程** | API数据采集、ETL管线、PostgreSQL性能优化 |
| **产品思维** | 需求分析 → 系统设计 → 交互设计 → 部署交付 |

---

## 附录

### A. 关键依赖版本

```
Python 3.11+
PostgreSQL 15
PostGIS 3.3
FastAPI 0.109+
React 18.2
deck.gl 9.0
XGBoost 2.0
Node.js 20
Docker 24+
```

### B. 参考资源

- [OGC CityGML 3.0 标准](https://www.ogc.org/standard/citygml/)
- [高德地图 Web服务 API 文档](https://lbs.amap.com/api/webservice/summary)
- [PostGIS 官方文档](https://postgis.net/documentation/)
- [deck.gl 官方文档](https://deck.gl/docs)
- [XGBoost 文档](https://xgboost.readthedocs.io/)
- [SHAP 文档](https://shap.readthedocs.io/)

### C. 同类研究参考

- 《基于POI数据的城市商业选址分析——以星巴克为例》（地理学报）
- 《Spatial Analysis of Coffee Shop Location Strategy Using GIS and Machine Learning》（Journal of Retailing and Consumer Services）
- 《快取型咖啡连锁店选址影响因素研究——以瑞幸咖啡为例》（经济地理）

---

> **文档版本**：v1.0 | **最后更新**：2026-07-18 | **作者联系**：见GitHub仓库
