# 🍵 瑞幸空间分析平台 — 程序模块说明

## 1. 项目总览

```
luckin-spatial-analysis/
├── manage.py              # Python 管理控制台（~600行，支持 start/stop/restart/status 等命令）
├── 控制台.bat              # Windows 双击启动交互式控制台
├── 启动服务.bat            # Windows 双击一键启动全部服务
├── 停止服务.bat            # Windows 双击一键停止全部服务
├── frontend/          # React + TypeScript + deck.gl 前端
│   └── src/
│       ├── pages/     # 5 个业务页面
│       ├── components/# 通用组件（MapView、StorePopup）
│       ├── hooks/     # 自定义 React Hooks
│       └── styles/    # 全局样式
├── backend/           # FastAPI + asyncpg 后端
│   ├── routers/       # API 路由（stores、analysis、prediction、geo）
│   ├── models/        # Pydantic 数据模型
│   ├── main.py        # 应用入口
│   └── database.py    # 数据库连接配置
├── scripts/           # Python 数据管道
│   ├── config.py      # 全局配置
│   ├── fetch_stores.py    # 门店数据采集
│   ├── fetch_pois.py      # POI 数据采集
│   ├── compute_features.py# 空间特征计算
│   ├── train_model.py     # XGBoost 模型训练
│   └── import_osm.py      # OSM 数据导入
├── sql/               # 数据库 DDL 和分析 SQL
│   ├── schema.sql     # 建表语句
│   ├── spatial_join.sql  # 空间分析函数
│   └── analytics.sql  # 分析物化视图
└── data/              # 数据目录（gitignore）
    ├── raw/           # 原始 JSON 数据
    ├── models/        # 训练好的模型文件
    └── luckin_spatial_backup.sql  # 数据库备份文件（34MB）
```

---

## 2. 前端模块

### 2.1 页面结构

| 页面 | 路由 | 文件 | 功能 |
|------|------|------|------|
| 总览 | `/` | `HomePage.tsx` | 全国门店分布热力图、统计卡片（瑞幸+星巴克对比） |
| 时空演变 | `/evolution` | `EvolutionPage.tsx` | 年度热力图、时间轴面积图、年份滑块、城市筛选 |
| POI分析 | `/poi-analysis` | `PoiAnalysisPage.tsx` | POI 雷达图、Lift 柱状图、详情表、缓冲半径切换 |
| 竞品对比 | `/comparison` | `ComparisonPage.tsx` | 瑞幸 vs 星巴克城市层级对比、共址率分析 |
| 选址预测 | `/prediction` | `PredictionPage.tsx` | 交互式地图选点、POI 密度评分、特征重要性图 |

### 2.2 核心组件

**MapView** (`components/MapView.tsx`)
- 封装 deck.gl + react-map-gl + maplibre-gl
- 支持 4 种图层：`scatterplot`、`heatmap`、`column`、`geojson`
- 受控模式：`viewState` 使用 React `useState` 管理，切换年份/参数时地图视角不再重置
- 支持点击回调（`onClick`）和悬浮弹窗（`StorePopup`），点击地图散点可查看最近门店详情
- 底图：高德中文瓦片

**StorePopup** (`components/StorePopup.tsx`)
- 门店悬浮卡片：品牌标签、地址、类型、开业日期、周边 POI 统计

**HomePage 地图交互**
- 点击地图散点自动查询最近门店详情（品牌、地址、周边 POI 统计）
- 调用 `GET /api/stores/nearest`（PostGIS KNN `<->` 运算符）

### 2.3 自定义 Hooks

| Hook | 文件 | 说明 |
|------|------|------|
| `useApi` | `useApi.ts` | 通用 GET 请求，自动管理 loading/error/abort（使用 AbortController 防止组件卸载后更新状态） |
| `useMutation` | `useApi.ts` | 通用 POST 请求 |
| `useCities` | `useCities.ts` | 城市列表及派生数据（totalLuckin、cityOptions 等） |
| `useTimeline` | `useStoreData.ts` | 门店月度时间轴 |
| `useHeatmap` | `useStoreData.ts` | 热力图 GeoJSON 数据 |
| `usePoiAnalysis` | `usePoiAnalysis.ts` | POI 关联 + Lift 值 |
| `useComparison` | `useComparison.ts` | 品牌对比 + 城市层级统计 |
| `usePrediction` | `usePrediction.ts` | 选址评分预测 |

---

## 3. 后端 API

### 3.1 路由概览

| 前缀 | 文件 | 说明 |
|------|------|------|
| `/api/stores` | `routers/stores.py` | 门店 CRUD、热力图、时间轴、城市列表 |
| `/api/analysis` | `routers/analysis.py` | POI 关联、Lift 值、品牌对比、城市层级 |
| `/api/prediction` | `routers/prediction.py` | 选址评分、城市推荐热力图 |
| `/api/geo` | `routers/geo.py` | 逆地理编码（经纬度→城市） |

### 3.2 关键 API 详情

**门店相关**
```
GET  /api/stores                    # 门店列表（分页、筛选）
GET  /api/stores/{id}               # 门店详情 + 周边POI
GET  /api/stores/nearest            # 根据坐标查找最近门店（PostGIS KNN <-> 运算符）
GET  /api/stores/heatmap/data       # 热力图 GeoJSON
GET  /api/stores/timeline/data      # 月度新增/累计时间轴
GET  /api/stores/cities/list        # 城市门店数量
```

**分析相关**
```
GET  /api/analysis/store-poi-correlation  # POI 关联统计（按类别/半径/城市）
GET  /api/analysis/lift-scores            # Lift 值（POI 富集程度）
GET  /api/analysis/brand-comparison       # 瑞幸 vs 星巴克对比
GET  /api/analysis/city-tiers-stats       # 城市层级渗透统计
```

**预测相关**
```
POST /api/prediction/score           # 单点选址评分
GET  /api/prediction/heatmap         # 城市选址推荐热力图（网格）
```

**地理编码**
```
GET  /api/geo/reverse                # 坐标→城市（调用高德 API）
```

### 3.3 评分公式

选址适宜度使用加权 POI 密度 + Sigmoid 归一化：

```
raw_score = office_500×0.25 + metro_500×0.20 + mall_500×0.15 
          + residential_500×0.15 + university_500×0.10 
          + restaurant_500×0.05 + convenience_500×0.05 + cafe_500×0.05

score = 1 / (1 + exp(-(raw_score - 1.5 + tier_bonus)))

tier_bonus = max(0, (5 - city_tier) × 0.05)
```

权重基于 Lift 分析：写字楼(18.3x) > 地铁(6.9x) > 商场(11.6x) > 住宅(11.1x) > 大学(5.6x)

---

## 4. 数据管道

### 4.1 执行流程

```
fetch_stores.py ──→ fetch_pois.py ──→ import_osm.py ──→ compute_features.py ──→ train_model.py
      ↓                  ↓                 ↓                  ↓                    ↓
   门店数据            POI 数据         OSM 补充数据        空间特征             XGBoost 模型
   stores 表          pois 表         pois 表追加       store_poi_stats 表     .pkl 文件

数据库备份: data/luckin_spatial_backup.sql (34MB) — 全量数据备份，可通过 psql 一键恢复
```

### 4.2 脚本说明

| 脚本 | 输入 | 输出 | 耗时 |
|------|------|------|------|
| `fetch_stores.py` | 高德 API | `stores` 表（21城，瑞幸 8,640 + 星巴克 4,366） | ~10分钟 |
| `fetch_pois.py` | 高德 API | `pois` 表（21城, ~32,468条） | ~50分钟 |
| `compute_features.py` | stores + pois | `store_poi_stats` 表（~500K条） | ~15分钟 |
| `train_model.py` | store_poi_stats | `xgboost_location_model.pkl` | ~3分钟 |

### 4.3 预计算缓存表

为提高查询性能（从 90+秒 → <0.1秒），以下数据预先计算：

| 缓存表 | 内容 | 更新时机 |
|--------|------|----------|
| `poi_correlation_cache` | 各城市/半径/品牌的 POI 平均密度 | 新增城市后 |
| `poi_lift_cache` | 各城市/半径/品牌的 POI Lift 值 | 新增城市后 |
| `brand_compare_cache` | 各城市瑞幸 vs 星巴克对比 | 新增城市后 |

## 5. 数据库 Schema

### 5.1 核心表

```
stores (id, name, brand, lng, lat, geom, city, district, open_date, ...)
pois   (id, name, category, lng, lat, geom, city, ...)
store_poi_stats (store_id, radius, poi_category, poi_count)  ← 关联表
city_tiers (city_name, tier, tier_label, population, gdp)
random_points (id, geom, city)  ← 负样本
```

### 5.2 物化视图

```
mv_city_stats  ← 城市门店统计
```

### 5.3 空间索引

```sql
CREATE INDEX idx_stores_geom ON stores USING gist(geom);
CREATE INDEX idx_pois_geom ON pois USING gist(geom);
CREATE INDEX idx_stores_brand_city ON stores(brand, city);
```

---

## 6. 技术栈速查

| 层 | 技术 | 版本 |
|----|------|------|
| 前端框架 | React + TypeScript | 18.2 / 5.3 |
| 构建工具 | Vite | 5.0 |
| UI 组件 | Ant Design | 5.12 |
| 地图渲染 | deck.gl + react-map-gl + maplibre-gl | 9.0 / 7.1 / 4.0 |
| 图表 | ECharts | 5.4 |
| 后端框架 | FastAPI | 0.109+ |
| 数据库驱动 | asyncpg | 0.29+ |
| 数据库 | PostgreSQL + PostGIS | 15.8 / 3.6.2 |
| 机器学习 | XGBoost + SHAP | 2.0 / 0.43 |
| 底图 | 高德中文瓦片 | — |
| 外部 API | 高德地图 Web 服务 | — |
