# 🍵 瑞幸空间分析平台 — 完整部署说明书

> 从零开始，本地开发环境部署指南

---

## 方式一：Windows 原生部署（当前环境）

### 第 1 步：安装 PostgreSQL 15 + PostGIS

**1.1 安装 PostgreSQL 15**

```powershell
# 下载安装包（约 354MB）
# https://get.enterprisedb.com/postgresql/postgresql-15.8-1-windows-x64.exe

# 安装参数：
# - 安装目录：C:\Program Files\PostgreSQL\15
# - 端口：5432
# - 超级用户密码：your_db_password_here
# - 区域设置：Chinese (Simplified), China
```

**1.2 安装 PostGIS 3.6**

```powershell
# 下载 PostGIS 捆绑包（约 119MB）
# https://download.osgeo.org/postgis/windows/pg15/postgis-bundle-pg15-3.6.2x64.zip

# 解压后，将以下内容复制到 PostgreSQL 15 目录：
#   bin/*.dll     → C:\Program Files\PostgreSQL\15\bin\
#   lib/*         → C:\Program Files\PostgreSQL\15\lib\
#   share/extension/* → C:\Program Files\PostgreSQL\15\share\extension\
#   share/contrib/     → C:\Program Files\PostgreSQL\15\share\contrib\
#   gdal-data/         → C:\Program Files\PostgreSQL\15\gdal-data\

# 复制 bin DLL 时需要先停止 PostgreSQL 服务：
net stop postgresql-x64-15
# 复制文件...
net start postgresql-x64-15
```

**1.3 创建数据库**

```bash
export PATH="/c/Program Files/PostgreSQL/15/bin:$PATH"
export PGPASSWORD="your_db_password_here"

# 创建数据库和用户
psql -U postgres -h localhost -c "CREATE DATABASE luckin_spatial;"
psql -U postgres -h localhost -c "CREATE USER luckin WITH PASSWORD 'your_db_password_here';"
psql -U postgres -h localhost -c "GRANT ALL PRIVILEGES ON DATABASE luckin_spatial TO luckin;"
psql -U postgres -h localhost -d luckin_spatial -c "GRANT ALL ON SCHEMA public TO luckin;"

# 启用 PostGIS 扩展
psql -U postgres -h localhost -d luckin_spatial -c "CREATE EXTENSION postgis;"
psql -U postgres -h localhost -d luckin_spatial -c "CREATE EXTENSION postgis_topology;"
psql -U postgres -h localhost -d luckin_spatial -c "CREATE EXTENSION postgis_raster;"

# 建表
psql -U luckin -h localhost -d luckin_spatial -f sql/schema.sql
psql -U luckin -h localhost -d luckin_spatial -f sql/spatial_join.sql
psql -U luckin -h localhost -d luckin_spatial -f sql/analytics.sql
```

### 第 2 步：配置环境变量

在项目根目录 `D:\ClaudeCodeFile\Test\luckin-spatial-analysis\` 创建 `.env`：

```env
AMAP_API_KEY=your_amap_api_key_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=luckin_spatial
POSTGRES_USER=luckin
POSTGRES_PASSWORD=your_db_password_here
```

### 第 3 步：安装 Python 依赖

```bash
# 安装 API 运行时
pip install fastapi uvicorn asyncpg aiohttp pydantic pydantic-settings

# 安装数据处理库
pip install numpy pandas scikit-learn xgboost shap joblib osmnx geopandas

# 或一键安装
pip install -r requirements-api.txt
pip install -r requirements.txt
```

### 第 4 步：安装前端依赖

```bash
cd frontend
npm install
```

### 第 5 步：启动数据库（Windows 服务已运行则跳过）

```powershell
net start postgresql-x64-15
```

### 第 6 步：初始化数据

**方式 A：使用管理控制台（推荐）**

```bash
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis

# 一键初始化数据库
python manage.py setup
```

**方式 B：从备份文件快速恢复（已有完整数据）**

```bash
# 如果已有备份文件，可直接恢复完整数据（含门店、POI、缓存）
psql -U luckin -h localhost -d luckin_spatial -f data/luckin_spatial_backup.sql
```

**方式 C：手动逐步骤采集（从零开始）**

```bash
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis

# 1. 采集门店数据（约 10 分钟）
python scripts/fetch_stores.py

# 2. 采集 POI 数据（约 50 分钟）
python scripts/fetch_pois.py

# 3. 计算空间特征（约 15 分钟）
python scripts/compute_features.py

# 4. 训练模型（约 3 分钟）
python scripts/train_model.py

# 5. 预计算缓存表（一次性，约 16 分钟）
# 见下方「补充：预计算缓存」脚本
```

### 第 7 步：启动服务（任选一种方式）

**方式 A：Python 管理控制台（推荐）**

```bash
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis

# 交互式菜单模式
python manage.py

# 或一键启动全部服务
python manage.py start
```

管理控制台支持的命令：
```bash
python manage.py start      # 启动全部服务（后端 + 前端）
python manage.py stop       # 停止全部服务
python manage.py restart    # 重启全部服务
python manage.py status     # 查看服务运行状态
python manage.py backend    # 仅启动后端
python manage.py frontend   # 仅启动前端
python manage.py setup      # 初始化数据库（建表 + 扩展）
python manage.py logs       # 查看服务日志
python manage.py open       # 在浏览器打开应用
```

**方式 B：双击 .bat 文件（Windows 最简方式）**

```
双击 启动服务.bat   → 一键启动全部服务
双击 停止服务.bat   → 一键停止全部服务
双击 控制台.bat     → 打开交互式管理控制台
```

**方式 C：手动启动（开发者常用）**

```bash
# 终端 1：启动后端
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000

# 验证：
#   http://localhost:8000/health → {"status":"ok"}
#   http://localhost:8000/docs    → Swagger API 文档

# 终端 2：启动前端
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis\frontend
npx vite --host

# 打开浏览器访问：
#   http://localhost:3000
```

---

## 方式二：Docker 部署（需要 WSL2）

### 前置条件

```powershell
# 安装 WSL2
wsl --install

# 安装 Docker Desktop
# https://www.docker.com/products/docker-desktop/
```

### 启动

```bash
cd D:\ClaudeCodeFile\Test\luckin-spatial-analysis

# 一键启动全部服务
docker compose up -d

# 前端：http://localhost:3000
# 后端：http://localhost:8000/docs
# 数据库：localhost:5432
```

**docker-compose.yml 包含：**
- `postgis` — PostgreSQL 15 + PostGIS 3.3
- `backend` — FastAPI 后端
- `frontend` — Vite + React 前端

### Docker 环境数据初始化

```bash
# 进入后端容器
docker exec -it luckin-backend bash

# 执行数据采集
python scripts/fetch_stores.py
python scripts/fetch_pois.py
python scripts/compute_features.py
python scripts/train_model.py
```

---

## 管理控制台

项目提供了 `manage.py`（~600行）作为统一的服务管理入口，支持交互式菜单和命令行两种模式。

### 交互式模式

```bash
python manage.py
```

进入交互式菜单后，显示所有可用命令及说明，输入命令即可执行。

### 命令行模式

```bash
python manage.py start      # 启动全部服务（后端 8000 + 前端 3000）
python manage.py stop       # 停止全部服务（清理进程 + 端口）
python manage.py restart    # 重启全部服务
python manage.py status     # 检查后端/前端/数据库运行状态
python manage.py backend    # 仅启动后端（uvicorn，端口 8000）
python manage.py frontend   # 仅启动前端（vite，端口 3000）
python manage.py setup      # 初始化数据库：建表 + 创建扩展 + 导入备份数据
python manage.py logs       # 实时查看后端 + 前端日志
python manage.py open       # 在默认浏览器打开 http://localhost:3000
```

### Windows 快捷方式

| 文件 | 功能 |
|------|------|
| `控制台.bat` | 双击打开 Python 交互式控制台 |
| `启动服务.bat` | 双击一键启动全部服务 |
| `停止服务.bat` | 双击一键停止全部服务 |

---

## 补充：预计算缓存脚本

为提高 POI 分析页面性能，首次部署后需运行以下 Python 脚本：

```python
# precompute_cache.py
import asyncpg, asyncio, time

DB = {'host': 'localhost', 'port': 5432, 'database': 'luckin_spatial',
      'user': 'luckin', 'password': 'your_db_password_here'}

async def main():
    conn = await asyncpg.connect(**DB)
    
    # 1. POI 关联缓存（约 2 分钟）
    print('1/3 Computing correlation cache...')
    await conn.execute('DROP TABLE IF EXISTS poi_correlation_cache')
    # ... (完整脚本见项目 scripts/ 目录)
    
    # 2. POI Lift 缓存（约 16 分钟，最耗时）
    print('2/3 Computing lift cache...')
    
    # 3. 品牌对比缓存（约 2 分钟）
    print('3/3 Computing brand comparison cache...')
    
    print('Done!')
    await conn.close()

asyncio.run(main())
```

---

## 启动验证清单

| 组件 | 验证方式 | 预期结果 |
|------|---------|---------|
| PostgreSQL | 服务 `postgresql-x64-15` 状态 Running | — |
| PostGIS | `SELECT PostGIS_Version();` | `3.6 USE_GEOS=1...` |
| 数据库 | `\dt` 显示 8+ 张表 | stores, pois, ... |
| 后端 | `curl http://localhost:8000/health` | `{"status":"ok"}` |
| 前端 | `curl http://localhost:3000` | HTTP 200 |
| API 文档 | 浏览器打开 `:8000/docs` | Swagger UI |
| 完整页面 | 浏览器打开 `:3000` | 5 个页面可切换 |

---

## 常用命令

```bash
# ========== 服务管理（manage.py）==========
python manage.py start      # 启动全部服务
python manage.py stop       # 停止全部服务
python manage.py restart    # 重启全部服务
python manage.py status     # 查看运行状态
python manage.py backend    # 仅启动后端
python manage.py frontend   # 仅启动前端
python manage.py setup      # 初始化数据库
python manage.py logs       # 查看日志
python manage.py open       # 浏览器打开应用

# ========== 手动管理 ==========
# 重启后端
# Ctrl+C 停止 → python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000

# 重启前端
# Ctrl+C 停止 → cd frontend && npx vite --host

# 重置数据库数据（保留结构）
psql -U luckin -h localhost -d luckin_spatial -c "TRUNCATE stores, pois, store_poi_stats, random_points, kde_grid CASCADE;"

# 从备份恢复数据库
psql -U luckin -h localhost -d luckin_spatial -f data/luckin_spatial_backup.sql

# 刷新物化视图
psql -U luckin -h localhost -d luckin_spatial -c "REFRESH MATERIALIZED VIEW mv_city_stats;"

# 仅重新训练模型
python scripts/train_model.py

# 新增城市后更新缓存
# 运行对应的城市数据采集 + 缓存更新脚本
```

---

## 故障排查

| 问题 | 原因 | 解决 |
|------|------|------|
| 后端 500 错误 | 数据库未运行 | `net start postgresql-x64-15` |
| 前端地图不显示 | Card body 高度为 0 | 确认 `styles={{ body: { height: '100%' } }}` |
| POI 分析很慢 | 缓存未初始化 | 运行预计算缓存脚本 |
| 预测全返回 99% | 模型过拟合 | 使用基于 POI 密度的评分公式 |
| 高德 API 超时 | DNS 解析问题 | 卸载 `aiodns`：`pip uninstall aiodns` |
| 城市选择无数据 | 城市名不匹配 | 确保 stores.city = city_tiers.city_name（都带"市"） |
